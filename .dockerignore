###############################
# Docker Ignore File          #
# Optimized for Node + Prisma #
###############################

# NEVER ignore these (needed for build):
#  - package.json
#  - package-lock.json
#  - tsconfig.json
#  - prisma/schema.prisma
#  - src/

# Dependencies (will be re-installed in image)
node_modules
npm-debug.log
yarn-error.log

# Build artifacts
dist
coverage

# Environment / secrets (provide via Render dashboard or docker build args)
.env
.env.*

# Logs
*.log

# Database seed script can be excluded from main build output but we DO send it (remove ignore)
# If you want to exclude it from context, uncomment next line:
# prisma/seed.ts

# System / editor files
.DS_Store
.vscode
.idea

# Git
.git
.gitignore